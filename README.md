# distributed_systemd_start
This package provides a systemd autostart service to start ROS in a distributed environment. An additional config script is supplied which will help you to install the service.
